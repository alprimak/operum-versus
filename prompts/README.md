# Agent Prompts

Each file contains the initial prompt to give to a specific AI coding system.
The prompt is identical in content — only adapted for the system's interface.

**Rules:**
- Copy-paste the prompt exactly — no modifications
- Do not add extra context or hints
- Start the timer immediately after pasting the prompt
- Count any additional guidance as a "human intervention"
